<Window x:Class="GuluPet.Presentation.BehaviorBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="咕噜的小动作"
        Width="760"
        Height="720"
        MinWidth="580"
        MinHeight="480"
        WindowStartupLocation="CenterScreen"
        Background="#FFF8F4"
        FontFamily="Microsoft YaHei UI"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Window.Resources>
        <Style x:Key="BehaviorButtonStyle"
               TargetType="Button">
            <Setter Property="Foreground"
                    Value="#FF493C36" />
            <Setter Property="Background"
                    Value="#FFFFFFFF" />
            <Setter Property="BorderBrush"
                    Value="#FFEADDD5" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Padding"
                    Value="16,12" />
            <Setter Property="Margin"
                    Value="0,0,0,8" />
            <Setter Property="HorizontalContentAlignment"
                    Value="Stretch" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Card"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="Card"
                                        Property="Background"
                                        Value="#FFFFF1EA" />
                                <Setter TargetName="Card"
                                        Property="BorderBrush"
                                        Value="#FFF2B8A0" />
                            </Trigger>
                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter TargetName="Card"
                                        Property="Background"
                                        Value="#FFFFE4D8" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Card"
                                        Property="BorderBrush"
                                        Value="#FFDF9E82" />
                                <Setter TargetName="Card"
                                        Property="BorderThickness"
                                        Value="2" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.48" />
                            </Trigger>
                            <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}"
                                         Value="True">
                                <Setter TargetName="Card"
                                        Property="Background"
                                        Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
                                <Setter TargetName="Card"
                                        Property="BorderBrush"
                                        Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
                                <Setter Property="Foreground"
                                        Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BehaviorCardTextStyle"
               TargetType="TextBlock">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}"
                             Value="True">
                    <Setter Property="Foreground"
                            Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BehaviorCardTitleTextStyle"
               TargetType="TextBlock"
               BasedOn="{StaticResource BehaviorCardTextStyle}">
            <Setter Property="Foreground" Value="#FF493C36" />
        </Style>

        <Style x:Key="BehaviorCardContextTextStyle"
               TargetType="TextBlock"
               BasedOn="{StaticResource BehaviorCardTextStyle}">
            <Setter Property="Foreground" Value="#FF745B51" />
        </Style>

        <Style x:Key="BehaviorCardDetailTextStyle"
               TargetType="TextBlock"
               BasedOn="{StaticResource BehaviorCardTextStyle}">
            <Setter Property="Foreground" Value="#FF8A7770" />
        </Style>

        <Style x:Key="BehaviorCardBadgeStyle"
               TargetType="Border">
            <Setter Property="Background" Value="#FFFFD4C1" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}"
                             Value="True">
                    <Setter Property="Background"
                            Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BehaviorCardBadgeTextStyle"
               TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FF71483A" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}"
                             Value="True">
                    <Setter Property="Foreground"
                            Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BehaviorFamilyComboBoxItemStyle"
               TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="11,8" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Foreground" Value="#FF4D403A" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemSurface"
                                Margin="2"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemSurface"
                                        Property="Background"
                                        Value="#FFFFEFE7" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemSurface"
                                        Property="Background"
                                        Value="#FFFFDDCF" />
                            </Trigger>
                            <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}"
                                         Value="True">
                                <Setter TargetName="ItemSurface"
                                        Property="Background"
                                        Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
                                <Setter Property="Foreground"
                                        Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
                            </DataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding Source={x:Static SystemParameters.HighContrast}}"
                                               Value="True" />
                                    <Condition Binding="{Binding IsHighlighted, RelativeSource={RelativeSource Self}}"
                                               Value="True" />
                                </MultiDataTrigger.Conditions>
                                <Setter TargetName="ItemSurface"
                                        Property="Background"
                                        Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                                <Setter Property="Foreground"
                                        Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" />
                            </MultiDataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BehaviorFamilyComboBoxStyle"
               TargetType="ComboBox">
            <Setter Property="Background" Value="#FFFFFFFF" />
            <Setter Property="Foreground" Value="#FF4D403A" />
            <Setter Property="BorderBrush" Value="#FFE8DBD3" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="11,0,36,0" />
            <Setter Property="ItemContainerStyle"
                    Value="{StaticResource BehaviorFamilyComboBoxItemStyle}" />
            <Setter Property="MaxDropDownHeight" Value="320" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="DropDownToggle"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Background="{TemplateBinding Background}"
                                          Foreground="{TemplateBinding Foreground}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="FilterSurface"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="12">
                                            <Path x:Name="Arrow"
                                                  Width="9"
                                                  Height="5"
                                                  Margin="0,0,13,0"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  Data="M 0 0 L 4.5 4.5 L 9 0"
                                                  Stroke="{TemplateBinding Foreground}"
                                                  StrokeThickness="1.5"
                                                  StrokeLineJoin="Round" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="FilterSurface"
                                                        Property="Background"
                                                        Value="#FFFFF4EE" />
                                                <Setter TargetName="FilterSurface"
                                                        Property="BorderBrush"
                                                        Value="#FFF2B8A0" />
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="FilterSurface"
                                                        Property="Background"
                                                        Value="#FFFFE9DE" />
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="FilterSurface"
                                                        Property="BorderBrush"
                                                        Value="#FFDF9E82" />
                                            </Trigger>
                                            <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}"
                                                         Value="True">
                                                <Setter TargetName="FilterSurface"
                                                        Property="Background"
                                                        Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
                                                <Setter TargetName="FilterSurface"
                                                        Property="BorderBrush"
                                                        Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
                                                <Setter TargetName="Arrow"
                                                        Property="Stroke"
                                                        Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
                                            </DataTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" />

                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PopupAnimation="Fade">
                                <Border x:Name="PopupSurface"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        Margin="0,4,0,0"
                                        Padding="4"
                                        Background="#FFFFFFFF"
                                        BorderBrush="#FFE4D2C7"
                                        BorderThickness="1"
                                        CornerRadius="12">
                                    <ScrollViewer CanContentScroll="True"
                                                  VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="DropDownToggle"
                                        Property="BorderBrush"
                                        Value="#FFDF9E82" />
                                <Setter TargetName="DropDownToggle"
                                        Property="BorderThickness"
                                        Value="2" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.48" />
                            </Trigger>
                            <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}"
                                         Value="True">
                                <Setter Property="Background"
                                        Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
                                <Setter Property="Foreground"
                                        Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
                                <Setter Property="BorderBrush"
                                        Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
                                <Setter TargetName="PopupSurface"
                                        Property="Background"
                                        Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
                                <Setter TargetName="PopupSurface"
                                        Property="BorderBrush"
                                        Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                CornerRadius="22"
                Padding="22,18"
                Margin="0,0,0,16">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0"
                                     EndPoint="1,1">
                    <GradientStop Color="#FFFFDCCF"
                                  Offset="0" />
                    <GradientStop Color="#FFFFF0D5"
                                  Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="咕噜的小动作"
                               FontSize="25"
                               FontWeight="SemiBold"
                               Foreground="#FF4D403A" />
                    <TextBlock Margin="0,7,0,0"
                               Text="搜索或选择一个动作，点击即可预览。"
                               FontSize="13"
                               Foreground="#FF796A62" />
                </StackPanel>
                <Border Grid.Column="1"
                        Width="76"
                        Height="54"
                        CornerRadius="27"
                        Background="#CCFFFFFF">
                    <TextBlock Text="ฅ^•ﻌ•^ฅ"
                               FontSize="17"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="#FF6A5148" />
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1"
              Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="170" />
            </Grid.ColumnDefinitions>
            <Border Background="#FFFFFFFF"
                    BorderBrush="#FFE8DBD3"
                    BorderThickness="1"
                    CornerRadius="12"
                    Margin="0,0,10,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="38" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="⌕"
                               FontSize="22"
                               Foreground="#FF9C877C"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                    <TextBox x:Name="SearchBox"
                             Grid.Column="1"
                             BorderThickness="0"
                             Background="Transparent"
                             Padding="2,10,10,10"
                             FontSize="14"
                             ToolTip="找找咕噜的小动作"
                             AutomationProperties.Name="搜索咕噜的小动作"
                             TextChanged="OnSearchTextChanged" />
                </Grid>
            </Border>
            <ComboBox x:Name="FamilyFilter"
                      Grid.Column="1"
                      Style="{StaticResource BehaviorFamilyComboBoxStyle}"
                      FontSize="13"
                      AutomationProperties.Name="小动作分类"
                      SelectionChanged="OnFamilySelectionChanged" />
        </Grid>

        <Border Grid.Row="2"
                Background="#66FFFFFF"
                BorderBrush="#FFE8DBD3"
                BorderThickness="1"
                CornerRadius="16"
                Padding="10">
            <ListBox x:Name="BehaviorList"
                     Background="Transparent"
                     BorderThickness="0"
                     HorizontalContentAlignment="Stretch"
                     ScrollViewer.CanContentScroll="True"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment"
                                Value="Stretch" />
                        <Setter Property="Padding"
                                Value="0" />
                        <Setter Property="Margin"
                                Value="0" />
                        <Setter Property="Background"
                                Value="Transparent" />
                        <Setter Property="BorderThickness"
                                Value="0" />
                        <Setter Property="Focusable"
                                Value="False" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource BehaviorButtonStyle}"
                                Tag="{Binding}"
                                AutomationProperties.Name="{Binding AutomationName}"
                                AutomationProperties.HelpText="{Binding TriggerDescription}"
                                Click="OnBehaviorClicked">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding DisplayName}"
                                               FontSize="15"
                                               FontWeight="SemiBold"
                                               Style="{StaticResource BehaviorCardTitleTextStyle}" />
                                    <TextBlock Margin="0,5,0,0"
                                               Text="{Binding TriggerDescription}"
                                               FontSize="12"
                                               LineHeight="18"
                                               Style="{StaticResource BehaviorCardContextTextStyle}"
                                               TextWrapping="Wrap" />
                                    <TextBlock Margin="0,4,0,0"
                                               Text="{Binding Detail}"
                                               FontSize="11"
                                               Style="{StaticResource BehaviorCardDetailTextStyle}"
                                               TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                                <Border Grid.Column="1"
                                        Margin="14,0,0,0"
                                        Padding="13,7"
                                        CornerRadius="12"
                                        Style="{StaticResource BehaviorCardBadgeStyle}"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="看看"
                                               FontWeight="SemiBold"
                                               Style="{StaticResource BehaviorCardBadgeTextStyle}" />
                                </Border>
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <Grid Grid.Row="3"
              Margin="4,14,4,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="CountText"
                       Foreground="#FF8A7770"
                       VerticalAlignment="Center" />
            <TextBlock x:Name="StatusText"
                       Grid.Column="1"
                       Margin="20,0,0,0"
                       Text="挑一个小动作看看"
                       TextAlignment="Right"
                       TextTrimming="CharacterEllipsis"
                       Foreground="#FF6F5B52"
                       AutomationProperties.LiveSetting="Polite"
                       VerticalAlignment="Center" />
        </Grid>
    </Grid>
</Window>
